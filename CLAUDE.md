# CLAUDE.md

The harness lives in **[AGENTS.md](./AGENTS.md)** — read it first.

State: `progress.md` (current state + next action) · `feature_list.json` (feature status).
Verify with `npm run verify` and `npx expo export --platform ios`.
